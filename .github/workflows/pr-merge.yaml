# Copyright 2022, Edge Case Research, Inc.
# Confidential

name: Test matrix
on:
  workflow_dispatch:

jobs:
  nums:
    name: "matrix ${{ matrix.dir }}"
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        num: ["1", "2", "3"]
    steps:
      - name: Echo matrix
        run: echo ${{ matrix.num }}
      - name: Echo no matrix
        run: echo "test"
